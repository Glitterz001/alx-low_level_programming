#include "main.h"

/**
 * main printing valid password
 * 
 * Return: 0 (Success)
 */

int main(void)
{
	char a
		a = a..z
		b = 1..9
	if (a)
		printf("valid password"\n)
	else 
		printf("print inavalid password"\n)

	return (0)
}
